import javax.swing.*;

public class TelaCadastro extends JFrame {
    public TelaCadastro() {
        setTitle("Tela de Login");
        setSize(500, 500);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        
        // Adicione componentes da tela de login aqui, como campos de usuário e senha, botão de login, etc.
    }
}
